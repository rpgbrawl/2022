import { MillisecToMinSecMilliPipe } from './millisec-to-min-sec-milli.pipe';

describe('MillisecToMinSecMilliPipe', () => {
  it('create an instance', () => {
    const pipe = new MillisecToMinSecMilliPipe();
    expect(pipe).toBeTruthy();
  });
});
